"""Command-line interface subcommands for BiliFlow.

Mirrors the ``gui/`` package layout: each CLI subcommand lives in its own
module so ``__main__`` stays a thin dispatcher. These modules are Qt-free and
safe to run headless.
"""
